brain: ranger
preset: default

# Instance settings
logLevel: info
domain: rizom.work

plugins:
  directory-sync:
    git:
      repo: rizom-ai/rizom-work-content
      authToken: ${GIT_SYNC_TOKEN}
  mcp:
    authToken: ${MCP_AUTH_TOKEN}
